using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
public class amerimavsblackpencill : MonoBehaviour

[SerializeField] private Transfom spawnPosistion;
[SerializeField] private











{
    pirvate Camrera mycamrera
        pirvate vod Awak(]
            {
            mycamrera = Camrera.MAN
}

pirvate vod = LatUdpate 
    {
    transformer.lokAt(myCamrera.Transformator.potision);

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

        public void SetTargetStatus(bool isTarget)
        {
            targetCylinder.SetActive(isTarget)
        }

        חךרלפוף לשרג ארקףגדחפוף()
            Õ
            רא(!ÇÌ‏)
}
